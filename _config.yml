# Site settings
title: blog
email: ajorr1@asu.edu
description: > # this means to ignore newlines until "baseurl:"
  I am a graduate student at ASU.
  I occasionally post my thoughts here.
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://www.adamjorr.com" # the base hostname & protocol for your site
permalink: /blog/:title
twitter_username: adamjorr
github_username:  adamjorr

# Build settings
markdown: kramdown

# Defaults
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      author: "Adam Orr"
